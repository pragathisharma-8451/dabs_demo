# The main pipeline for default_python
resources:
  pipelines:
    default_python_pipeline:
      name: default_python_pipeline_1
      target: default_python_${bundle.environment}
      libraries:
        - notebook:
            path: ../src/dlt_pipeline.ipynb

      configuration:
     
